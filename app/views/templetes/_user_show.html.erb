<h1>ユーザー詳細</h1>
<P>アバター画像</P>
<% if @current_user.avatar_image.present? %>
    <%= image_tag @current_user.avatar_image.url, class:"img-fluid circle", alt: "画像が表示できません", size: "300x300" %>
<% else %>
    <%= image_tag "no_image.jpg", class:"", alt: "画像が表示できません", size: "300x300" %>
<% end %>
<p>名前</p>
<p><%= @current_user.name %></p>
<span><%= @current_user.follow.count %> <%= link_to "フォロー", users_path(follow_or_follower: "follow") %></span>
<span><%= @current_user.follower.count %> <%= link_to "フォロワー", users_path(follow_or_follower: "follower") %></span>
<p><%= link_to "編集", edit_user_path(1), class: "btn btn-primary" %></p>